# platform :ios, '9.0'

target 'Flash Chat iOS13' do
  use_frameworks!
    
  # Pods for Flash Chat iOS13
  
  # Pod for animation at the main screen
  pod 'CLTypingLabel'
  
  # Pods for firebase
  pod 'Firebase/Auth'
  pod 'Firebase/Firestore'

end
